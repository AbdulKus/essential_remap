name: Apply root setup backend

on:
  push:
    branches: [feature/root-setup-mode]
    paths:
      - .github/scripts/apply_root_backend.py
      - .github/workflows/apply-root-mode.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/root-setup-mode
      - name: Apply backend changes
        run: python .github/scripts/apply_root_backend.py
      - name: Commit backend
        run: |
          git config user.name "free_vibrancy"
          git config user.email "73951988+AbdulKus@users.noreply.github.com"
          git add -A
          git commit -m "Add root setup backend"
          git push origin HEAD:feature/root-setup-mode
